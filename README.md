# Python Debugging Toolkit

A lightweight Python debugging tool that traces execution, inspects variables, and logs runtime errors.

## Features
- Line-by-line execution tracing
- Variable inspection
- Error logging to JSON
- CLI interface

## Usage

```bash
python main.py examples/buggy_program.py --trace --vars